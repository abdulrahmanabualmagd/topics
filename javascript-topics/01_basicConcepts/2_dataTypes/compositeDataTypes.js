// Object
let person = {
    name: 'John',
    age: 30,
    isMarried: false
};

// Array
let numbers = [1, 2, 3, 4, 5];
let fruits = ['apple', 'banana', 'orange'];

// Function
function add(a, b) {
    return a + b;
}

// Date
let currentDate = new Date();

// RegExp
let regex = /hello/i; // case-insensitive match for 'hello'

// Map (added in ECMAScript 6)
let myMap = new Map();
myMap.set('name', 'John');

// Set (added in ECMAScript 6)
let mySet = new Set();
mySet.add(1);
mySet.add(2);
