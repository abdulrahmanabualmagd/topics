/*
 * The .forEach() method executes a callback function on each of the elements
 * in an array in order.
 */
const numbers = [28, 77, 45, 99, 27];

numbers.forEach((number) => {
  console.log(number);
});
