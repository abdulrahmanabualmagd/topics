#if DEBUG_LEVEL == 2
    // Debugging code for level 2
#elif DEBUG_LEVEL == 1
    // Debugging code for level 1
#else
    // Default debugging code
#endif

/*
These directives are used for conditional compilation based on arithmetic expressions.
*/